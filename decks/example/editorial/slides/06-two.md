<!-- _class: two -->
<div>

## 좌우 2단 — `two`
- `<!-- _class: two -->` + `<div>` 두 개
- 텍스트 ↔ 코드 나란히
- 한쪽에 설명, 한쪽에 예시

</div>
<div>

```js
const bodies = files.map(readSlide);
const deck = header + bodies.join('---');
build(deck);   // → dist/example.html
```

</div>
